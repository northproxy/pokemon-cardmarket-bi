# data/exports/ (LOCAL ONLY — gitignored)

Ad hoc CSV exports from BI views (vw_collection_current_value,
vw_collection_summary, etc.) for your own use — reporting, sharing, or
just eyeballing the data outside a SQL client.

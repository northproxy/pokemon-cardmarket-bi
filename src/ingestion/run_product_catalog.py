"""
Entrypoint for the twice-monthly product catalog pipeline (called from
.github/workflows/product-catalog.yml).

If either source file fails, products must be left untouched and this run
should fail entirely rather than partially update the catalog. See docs/04.

TODO (Phase 4): implement
"""

if __name__ == "__main__":
    raise NotImplementedError("Phase 4: implement product catalog pipeline")
